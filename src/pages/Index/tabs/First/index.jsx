import React, { PureComponent } from 'react';

export default class First extends PureComponent{

    render() {
        return (
            <div>
                first
            </div>
        );
    }
}

